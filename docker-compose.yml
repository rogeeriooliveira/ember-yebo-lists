version: '2'
services:
  web:
    build: .
    command: yarn start
    volumes:
      - .:/usr/local/src/
      - node_modules:/usr/local/src/node_modules
      - tmp:/usr/local/src/tmp
    ports:
      - 49152:49152
      - 4200:4200

volumes:
  node_modules: {}
  tmp: {}

